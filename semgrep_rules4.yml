rules:
  - id: eval-use
    patterns:
      - pattern: eval(...);
      - pattern-not: eval('...');
           - pattern-not: eval(f"")
   message: eval detected
   languages:
      - php
    severity: ERROR
